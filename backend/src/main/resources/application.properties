spring.application.name=DockerPlatformBackend
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:dockerplatform}
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.show-sql=true

#elasticsearch
spring.elasticsearch.uris=http://${ELASTIC_HOST:elasticsearch}:${ELASTIC_PORT:9200}
spring.elasticsearch.host=${ELASTIC_HOST:elasticsearch}
spring.elasticsearch.port=${ELASTIC_PORT:9200}
spring.elasticsearch.username=${ELASTIC_USERNAME:elastic}
spring.elasticsearch.password=${ELASTIC_PASSWORD:C8*040*jyBxjsogT*wLD}

jwt.issuer=docker
jwt.secret=MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNA==

# registry config
registry.jwt.issuer=${REGISTRY_JWT_ISSUER:dockerplatform-backend}
registry.jwt.keyId=${REGISTRY_JWT_KEY_ID:registry-key-1}
registry.jwt.privateKeyPath=${REGISTRY_JWT_PRIVATE_KEY_PATH:/app/registry-private.pem}
#registry.jwt.privateKeyPath=../registry-auth/registry-private.pem



# Redis Cache Configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=60000
spring.cache.type=redis
spring.cache.redis.time-to-live=3600000
spring.cache.redis.cache-null-values=false
